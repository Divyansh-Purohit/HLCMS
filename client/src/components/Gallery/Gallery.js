import Card from "../UI/Card/Card";
const Gallery = () => {
  // const imgs = [1, 2, 3];
  const emptyMsg = "Page under development";
  return <Card content={emptyMsg} />;
};

export default Gallery;
